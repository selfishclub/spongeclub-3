# 매장 사진 넣는 곳

여기에 사진 파일을 넣고, `lib/stores.ts` 의 `image` 에 파일명을 적으면 갤러리에 나옵니다.

```ts
{ name: '인천계양점', region: '수도권', image: 'gyeyang.jpg' }
```

`image` 를 비워두면 회색 배경 + 매장명으로 표시됩니다. 사진이 준비되는 대로 하나씩 채우면 됩니다.

- 권장 비율: 4:3 (예: 800×600)
- 파일명은 영문·숫자로 (한글 파일명은 배포 환경에 따라 깨질 수 있습니다)
